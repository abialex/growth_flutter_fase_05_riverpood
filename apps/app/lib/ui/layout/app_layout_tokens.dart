/// Layout constraints shared by the application screens.
abstract final class AppLayoutTokens {
  /// Maximum width used by content-heavy screens.
  static const double contentMaxWidth = 960;

  /// Maximum width used by authentication forms.
  static const double formMaxWidth = 560;

  /// Width below which side-by-side controls become stacked.
  static const double compactBreakpoint = 600;
}
