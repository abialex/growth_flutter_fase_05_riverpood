import 'package:growth_flutter_fase_05_riverpood/ui/notifiers/logout/logout_state.dart';

/// Indicates that no logout request has started.
class LogoutInitialState extends LogoutState {
  /// Creates the initial logout state.
  const LogoutInitialState();
}
